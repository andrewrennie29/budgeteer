<h3>selection form for date / period</h3>
<h4>ajax the form to redraw accordion</h4>


<h3>Accordion to group by week</h3>

<%= render 'reports/card_total',  locals: { budget: @daily_budget, expenses: @variable_expenses } %> <p>

<% @budgets.each_slice(2) do |budgets| %>
  <div class="row">
    <% budgets.each do |budget| %>
     		<div class = "col-sm-6 report-card-<%= budget.id %>">
     		<%= render 'reports/card',  budget: budget %>
     		</div>
    <% end %>
  </div>
  <p>
<% end %>